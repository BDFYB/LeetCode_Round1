#include "solution.h"

std::string Solution::longest_common_perfix(std::vector<std::string> &strs) {
    return "xx";

}

int main(int argc, char *argv[]) {
    char *data = "bcd";
    std::cout << *++data << std::endl;
    const char* temp[] = {"a:", "b:", "c:"};
    std::vector<std::string> strms(temp, temp+3);
    for (std::string &s : strms) {
        std::cout << s << std::endl;
    }

    return 0;
}
